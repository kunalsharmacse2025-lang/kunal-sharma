#include<stdio.h>
int main()
{
    int i,pos,n,arr[];
    printf("Enter the array:");
    for(i=0;i<=n-1;i++)
    {
        scanf("%d",&a[i]);
    }
    printf("Enter the position:");
    scanf("%d",&pos);S
}